# TSLping 插件配置文件

# 插件前缀配置
prefix: "&6[TSL喵]&r "

# 功能设置
settings:
  entries_per_page: 10
  ping_colors:
    green: 100    # < 100ms 显示绿色
    yellow: 200   # 100-200ms 显示黄色
    # > 200ms 显示红色

# 分页设置
pagination:
  show_navigation: true
  navigation_clickable: true
